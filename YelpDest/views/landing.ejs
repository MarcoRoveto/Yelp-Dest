<!DOCTYPE html>
<html>
    <head>
        <title>YelpDest</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body id="body-landing"> <!--To target the landing page background only-->
        
    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">YelpDest!</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <% if(!currentUser){ %>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Sign Up</a></li>
                   <% } else { %>
                        <li><a href="#">Signed In As: <%= currentUser.username %></a></li>
                        <li><a href="/logout">Log Out</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error%>
            </div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= success%>
            </div>
        <% } %>
    </div>


<div id="landing-content" class="container">
        <div class="container">
            <h1>Welcome To YelpDest!</h1>
            <h4>Read/Post reviews about destinations from all over the world.</h4>
            <p>
                <a class="btn btn-primary btn-large" href="/destinations">View all Destinations!</a>
            </p>
        </div>
</div>

<% include ./partials/footer %>